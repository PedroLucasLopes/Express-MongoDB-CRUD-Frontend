import styled from 'styled-components';

const Title = styled.h1`
    font-weight: 600;
    font-size: 1.7rem;
    margin-bottom: 1rem;
`

const Main = styled.main`
    margin: 1.5rem .5rem;
`

export { Title, Main }