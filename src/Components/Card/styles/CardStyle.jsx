import { Card, Typography } from '@mui/material';
import styled from 'styled-components';

const CardBox = styled(Card)`
  margin-bottom: 2rem;
`

const Price = styled(Typography)`
  font-weight: 600;
  margin-top: 2rem;
`

export { CardBox, Price };
